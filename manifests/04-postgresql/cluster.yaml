apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: nextcloud-db
  namespace: nextcloud
spec:
  instances: 2

  bootstrap:
    initdb:
      database: nextcloud
      owner: nextcloud

  storage:
    size: 2Gi
