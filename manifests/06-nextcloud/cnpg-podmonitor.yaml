apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cnpg-cluster-monitor
  namespace: nextcloud
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: nextcloud-db
  podMetricsEndpoints:
  - port: metrics
    interval: 10s
